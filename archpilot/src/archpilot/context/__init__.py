"""Ambient context gathered before a prompt is sent.

This is what separates ArchPilot from a chatbot in a box: the machine already
knows which window is focused, what just failed in the terminal, and what other
Claude sessions are doing. Making the user retype that is the actual friction.
"""
