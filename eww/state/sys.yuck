(defpoll net_rate
  ;; {"down":KB/s,"up":KB/s,"iface":"..."} - the script takes its delta against
  ;; the previous call rather than sleeping, so this interval is real.
  :initial `{"down":0,"up":0,"iface":""}`
  :interval "1s"
  :run-while is_control_center_open
  "scripts/sys/net")

