(defvar is_player_open false)
(defvar is_telegram_open false)
(defvar is_notifications_open false)
(defvar is_audio_open false)
(defvar is_network_open false)
(defvar is_calendar_open false)
(defvar is_control_center_open false)

(include "./widgets/player.yuck")
(include "./widgets/telegram.yuck")
(include "./widgets/notifications.yuck")
(include "./widgets/audio.yuck")
(include "./widgets/network.yuck")
(include "./widgets/calendar.yuck")
; Included here rather than from the widget that uses them: eww's `include` is
; an unguarded textual splice, so a file pulled in from two places defines
; every variable in it twice and the whole config fails to load.
(include "./widgets/control_center/net.yuck")
(include "./widgets/control_center/window.yuck")
(include "./widgets/stickers.yuck")


; hyprland.conf -> sets SUPER + M to reload eww and eww open --toggle test
(defwindow test
  :monitor 0
  :geometry ( geometry :width "10%" :height "10%" :anchor "bottom left")
  "Tested Ok")
